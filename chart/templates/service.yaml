apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.units.service.name }}
spec:
  ports:
    -
      name: http
      port: {{ .Values.units.service.port.default }}
      targetPort: {{ .Values.units.service.port.target }}
  selector:
    app: {{ .Values.units.application.name }}
